# Copy to .env.local for local canary runs. In Cloud Run Jobs these are
# injected via Terraform from Secret Manager — do not commit real values.

CANARY_MODE=probe

# Gloo AI platform credentials (dedicated "canary" pair; do not reuse personal keys)
GLOO_AI_CLIENT_ID=
GLOO_AI_CLIENT_SECRET=

# Slack Web API (xoxb- bot token; channel ID, e.g. C0AU2FM2Q86)
ALERTS_SLACK_BOT_TOKEN=
ALERTS_SLACK_CHANNEL_ID=

# GCS bucket where run artifacts + state file live. Also stores the
# `state/model-registry-snapshot.json` blob used to diff the V2 model
# list between runs — no separate datastore needed.
CANARY_RESULTS_BUCKET=glooai-canary-results

# Better Stack heartbeat URL for this job's status-page component
# (optional — heartbeats are a no-op when unset; never set for local
# runs unless you intend to flip the real status page).
# CANARY_HEARTBEAT_URL=

# Ingestion mode only (CANARY_MODE=ingestion). The publisher must be a
# dedicated canary publisher owned by the canary client's org, and the
# org needs the `ingestion_access` entitlement.
CANARY_INGESTION_PUBLISHER_ID=
# Optional overrides (defaults: 600000 / 15000)
# CANARY_INGESTION_SLA_MS=600000
# CANARY_INGESTION_POLL_INTERVAL_MS=15000
